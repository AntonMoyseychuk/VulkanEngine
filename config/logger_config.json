{
    "loggers": {
        "application": {
            "name": "APP",
            "pattern": "[%^%L%$] [%n]: %v"
        },

        "graphics_api": {
            "name": "VULKAN",
            "pattern": "[%^%L%$] [%n]: %v"
        },

        "window_system": {
            "name": "GLFW",
            "pattern": "[%^%L%$] [%n]: %v"
        }
    }
}